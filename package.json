{
  "name": "prayer",
  "version": "0.0.3",
  "private": true,
  "description": "Wholehearted - a private prayer journal",
  "scripts": {
    "build": "node build.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  }
}

'use strict';
// Fixed, public-source diagnostics only. No credentials are read or printed.
(async()=>{try{const r=await fetch('https://static.esvmedia.org/crossref/crossref.min.js',{signal:AbortSignal.timeout(10000)});const s=await r.text();console.log('PUBLIC_ESV_SCRIPT_STATUS',r.status);console.log('PUBLIC_ESV_SCRIPT_HEAD',s.slice(0,1800));const m=s.match(/BASE_IFRAME_URL\s*=\s*['"]([^'"]+)/);if(!m)throw Error('Missing embed source');const base=new URL(m[1]);if(!['www.esv.org','esv.org'].includes(base.hostname))throw Error('Unexpected embed source');const url=base.href+'Luke%2022%3A42/?body_font_size=18';const page=await fetch(url,{signal:AbortSignal.timeout(10000)});const html=await page.text();console.log('EMBED_CHECK',JSON.stringify({url,status:page.status,headers:Object.fromEntries([...page.headers].filter(([k])=>['content-type','x-frame-options','content-security-policy'].includes(k))),hasPassage:html.includes('not my will'),head:html.slice(0,5000)}));}catch(e){console.log('SOURCE_PROBE_UNAVAILABLE',e.message);process.exitCode=1;}})();
